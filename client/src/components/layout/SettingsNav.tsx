import {
  NavigationMenu,
  NavigationMenuList,
} from "@/components/ui/navigation-menu";

export function SettingsNav() {
  const menuItems = [];

  return (
    <NavigationMenu>
      <NavigationMenuList className="flex-wrap gap-2">
        {/* Les éléments de navigation ont été supprimés */}
      </NavigationMenuList>
    </NavigationMenu>
  );
}